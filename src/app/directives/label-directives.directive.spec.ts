import { LabelDirectivesDirective } from './label-directives.directive';

describe('SpanDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new LabelDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
